package skillbill.contracts.workflow.payload

object WorkflowTimestampPayloadKeys {
  const val TIMESTAMP = "timestamp"
  const val FIRST_STARTED_AT = "first_started_at"
  const val HEARTBEAT_AT = "heartbeat_at"
  const val EXPIRES_AT = "expires_at"
}
