# SKILL-241 subtask 1: Replace audit with stateless attempts and minimal results

## Scope

Implement the complete parent behavior in one change. Each audit attempt checks all planned ACs, repairs code and test gaps in its own session, and returns the remaining ACs or an explicit empty list. After normal process completion, runtime finishes on an empty list or starts another fresh audit agent with nonempty result text. Process failure, interruption or missing output stops the loop. Resume repeats the full check without audit-specific persistence. The parent Minimal result section defines process-outcome and remaining-AC handling without status words.

Start from main. SKILL-240's feature branch is investigation context only and is not a prerequisite. Main currently contains an audit-gap backward edge to implement; remove it and its active consumers. If cycle machinery has since landed, remove its launch, settlement, recovery, status, telemetry, and tool dependencies in this same change.

## Implementation approach

1. Trace the audit path through phase declarations, prompt composition, launch, output handling, transitions, resume, status, and downstream projections. Keep the existing planned criterion list as the input authority.
2. Replace audit instructions with the complete code-and-tests check and same-session repair. Start a fresh provider session for each attempt. Runtime handles sequential retries after nonempty AC results by passing that text unchanged and full planning list to the next audit agent. Remove nested delegation, output-correction agents, and audit-to-implement repairs.
3. Separate process outcome from result content as described in the parent. Normal completion with an explicit empty list finishes audit; nonempty AC text requests another attempt. Process failure, interruption and missing output stop the loop. Accept ordinary list presentation without requiring status words or validating AC items against a report schema. Remove audit-stage acknowledgements, exact saved-value requirements, saved progress and audit repair checkpoints. Only the next audit agent receives the unresolved-AC text.
4. Disconnect and delete obsolete audit-specific runtime code and prompt contracts. Preserve only database-opening compatibility and ordinary historical workflow records. Old incomplete audits restart under the new behavior without an audit-state reset.
5. Add the boundary regressions below, update governed instructions and architecture documentation, and run the applicable checks. Keep production fixes and tests in this one subtask.

Likely owners on main include FeatureTaskRuntimePhaseWorkflowTransitions, FeatureTaskRuntimePhaseWorkflowProjectionDeclarations, FeatureTaskRuntimePhasePromptDirectives, FeatureTaskRuntimeRunLoopDrive, FeatureTaskRuntimeRunLoopPhaseRunner, FeatureTaskRuntimeRunLoopOutputVerification, FeatureTaskPhaseSettlementService, and runtime status and telemetry consumers. This is an investigation starting point, not a file allowlist.

## Acceptance criteria

1. Audit receives the complete acceptance-criteria list established during planning, with its existing identifiers and wording. Every attempt checks all criteria against the current repository. On an automatic retry it also receives the previous attempt's unresolved-AC text as a focus hint, never as a replacement for the complete list or proof that other criteria remain satisfied.
2. For every criterion, the audit agent locates the implementation that provides the required behavior and test cases whose assertions verify it. Missing implementation, missing tests, and tests that do not exercise the criterion are gaps. The agent writes or corrects code and tests in its own session and checks the result before returning. One meaningful test may cover several criteria; names, empty tests, and tautological assertions are not coverage.
3. Each audit attempt runs in one fresh agent session. The agent cannot spawn nested agents or delegate repair. It returns the remaining ACs, or an explicit empty list when none remain. Runtime alone owns the sequential retry loop: a normally completed attempt with nonempty result text starts a new audit agent with that text and the full planned AC list. No audit failure routes back to implement or to a separate repair agent.
4. Process outcome and audit content are separate signals. A normally completed process with an explicit empty AC list finishes audit; a normally completed process with nonempty remaining-AC text requests the next audit attempt. Accept ordinary bullets, numbered lists, plain text and empty-list notation with normal whitespace or Markdown presentation. Require no exact status words, versioned JSON envelope, AC-item schema, identifier membership check, evidence fields, exact saved value, or checkpoint proof. Forward nonempty result text unchanged for the next agent to interpret.
5. Cancellation, timeout, abnormal process exit, absent final output and whitespace-only output stop through ordinary interruption or failure handling with a concrete reason. They never count as an empty AC list and never trigger an automatic retry from an unproven result. Process outcome takes precedence over text emitted before a crash. Read the provider's completed final response, not incidental list syntax in progress messages. Missing planning input or an external blocker uses the existing workflow interruption path without requiring an agent-emitted status word.
6. An interrupted audit resumes with a fresh session and the full planned AC list, starting verification from the beginning. Existing repository edits remain for inspection and reconciliation. No saved result, partial criterion check, provider continuation session, repair receipt, or checkpoint may skip verification or settle the resumed audit. The previous failed attempt body is an in-memory hint only during an uninterrupted retry loop; losing it does not prevent resume.
7. Audit uses no audit-specific database or file persistence for progress or retries. Remove cycle revisions, launch bindings, gap memory, repair plans, pause grants, audit checkpoints, audit-stage acknowledgements, and per-stage telemetry from its execution path. Ordinary workflow status, attempt attribution, terminal outcome and process diagnostics remain allowed, but are not audit continuation inputs. No approval gate is inserted between audit attempts.
8. Review remains gated by a normally completed audit with an explicit empty remaining-AC list. Only the next audit attempt receives nonempty result text. Review, validation, implementation and other downstream agents receive no audit findings, repair instructions, or audit result projection. Ordinary resume of old incomplete audit or audit-gap runs uses the new loop without requiring legacy audit state or a reset; already completed workflows retain their historical status. Delete obsolete active audit machinery and retain only compatibility needed to open existing databases.
9. Audit inspects implemented behavior and written test cases; it does not require tests or builds to execute during audit. Their execution remains with existing downstream owners. Preserve review behavior, validation routing, worker cancellation and lease safety, and runtime-owned commits and pushes. Update source documentation and regenerate affected skill output when implementation changes governed source or rendering.
10. Boundary regressions prove an explicit empty list after normal completion advances, nonempty AC text launches exactly one next audit attempt with unchanged text and all planned ACs, and interruption or missing output stops without advancement or automatic relaunch. They cover ordinary list and plain-text presentation without status words, interrupted processes that printed an empty list, repair within each session, regression detection across attempts, full restart after interruption, no audit-state access, and no audit-to-implement or downstream findings handoff. No test requires a structured audit report validator.

## Non-goals

Do not implement the SKILL-240 durable cycle, redesign planning, remove ordinary workflow persistence, introduce an audit report schema, or change downstream gate ownership. Do not launch nested subagents from audit. Do not run the live SKILL-240 workflow or alter its database records to demonstrate acceptance.

## Dependency notes

No prior subtask or external issue is required. Read the parent specification for the stateless boundary and apply it directly to the current starting tree. Existing implementation edits after interrupted execution must remain available for inspection and reconciliation.

## Validation strategy

- Same-session repair regression. One criterion lacks implementation and another lacks meaningful test coverage. One audit launch fixes both and reaches ordinary completion without a repair-agent launch or stage acknowledgement.
- Restart regression. Interrupt after a partial edit, change previously checked code or tests, then resume. A fresh session receives and rechecks every criterion, preserves useful edits, and detects the regression rather than restoring an old satisfied result.
- Persistence independence regression. Audit and status operate with audit-specific storage access rejected and with legacy audit pause or cycle state present. Only ordinary workflow bookkeeping is written; no audit progress record is needed.
- Retry and routing regression. A nonempty AC result followed by an explicit empty list starts two sequential fresh audit sessions. The second gets the unchanged nonempty result and every planned AC. No implement or nested repair agent launches, and review gets no audit result projection.
- Terminal-result regression. Interruption, cancellation, timeout, abnormal exit after printing an empty list, missing final output and whitespace-only output stop without advancement or automatic relaunch. Bullets, numbered lists and plain-text AC descriptions pass through unchanged without status words. Whitespace or Markdown around an empty list is accepted; incidental brackets in progress or nonempty explanations cannot signal completion.

Prefer production orchestration boundaries with disposable workflow stores and a controlled launcher. Do not substitute exact prompt wording assertions for these behaviors. Run the relevant suites and manifest-routed quality checks, keeping build-only sessions within their declared commands. Update or remove tests that require deleted audit protocols instead of preserving those protocols to satisfy old tests.

## Next path

After implementation and required checks, the runtime handles its normal review, validation, history, and commit flow. No later audit-remediation subtask is planned.
